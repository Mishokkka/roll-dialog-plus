{
  "name": "fbl-roll-dialog-plus",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/dev-check.mjs && node --test",
    "smoke": "node tests/browser-smoke.mjs"
  }
}
